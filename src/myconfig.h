#pragma once

// ── Project identity ─────────────────────────────────────────────────────────
// Used by mDNS and the web server (if you add one).
// Device will be reachable at http://<WIFI_HOSTNAME>.local
#define WIFI_HOSTNAME "hb9iiu"
